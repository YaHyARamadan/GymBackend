# Gym Management SaaS — تعليمات التنفيذ الرئيسية

هذا المشروع نظام SaaS متعدد المستأجرين (Multi-tenant) لإدارة الجيمات، بمعمارية Supervisor → Owner → Branch Manager → Coach/Reception → Player.

**اقرأ `backend.md` و`frontend.md` كاملين قبل ما تبدأ أي سطر كود.** هما مش ملخص، هما المواصفة الفعلية الكاملة المتفق عليها بعد نقاش تفصيلي طويل — أي قرار فيهم (زي: القفل الفوري، إخفاء 404 بدل 403 للـ Sold License، عدم إشعار المنشأة بدخول السوبرفايزر) مقصود ومتفق عليه، مش افتراض احتياطي، وممنوع تغييره من غير رجوع لصاحب المشروع.

---

## ترتيب التنفيذ المقترح

1. **Backend أولًا**: Domain → Application (بالـ Validators والـ Handlers) → Infrastructure (DbContext + Migrations) → API (Controllers + Middleware) → Error Handling كامل → Docker

2. **بعد ما الباك اند يخلص تمامًا**، لازم تعمل ملف `API-CONTRACT.md` في جذر المشروع، يوثّق فيه:
   - كل endpoint موجود (method + path)، مقسّم حسب الـ Feature (Facilities, Supervisor, AuditLog, Contracts, Owners, Branches, Players, Payments...)
   - شكل الـ request body وshape الـ response لكل endpoint (مثال JSON مختصر، مش كل الحقول لو الموديل كبير — بس اللي الفرونت محتاجه فعليًا)
   - شكل الأخطاء الموحّد (من قسم Error Handling) مرة واحدة بالأول، مش مكرر تحت كل endpoint
   - أي Header مطلوب (Authorization, Idempotency-Key) وإمتى يتبعت
   - الفرق بين توكن الدخول العادي وتوكن الـ impersonation، وإزاي الفرونت يفرّق بينهم
   - قائمة كل الـ error codes المخصصة (زي `IMPERSONATION_EXPIRED`) وإيه اللي المفروض الفرونت يعمله عند كل واحد

   **الهدف من الملف ده**: لما تيجي تنفّذ الفرونت، تقرا `API-CONTRACT.md` بس (ملف واحد مختصر) بدل ما تفتح كل ملفات الباك اند وتقرا الكود كله من جديد — بيوفر context/tokens كتير ويخليك تركز على الفرونت نفسه.

3. **Frontend بعدها**: اقرا `frontend.md` + `API-CONTRACT.md` (مش كود الباك اند)، ابدأ بمودال الدخول والداشبورد الأساسي، بعدين Supervisor، بعدين باقي الصفحات
4. لا تنتقل لصفحة/فيتشر تاني قبل ما اللي قبله يغطي الأربع حالات المطلوبة في قسم اختبار الأخطاء (`backend.md` §5.3)

---

## قواعد عامة تنطبق على الاتنين

- **الأمان أولوية فوق السرعة**: أي قرار فيه شك بين "أسرع" و"أأمن"، اختار الأأمن والتزم بيه، ولو محتاج وقت أطول.
- **مفيش حل مؤقت (TODO/hack) يتسيب في كود الأمان أو الـ Multi-tenancy تحديدًا.** أي جزء تاني ممكن يتسجل كـ `// TODO` واضح مع سبب، لكن عزل البيانات بين المنشآت والتحقق من الصلاحيات لازم يكونوا كاملين من أول commit.
- **استخدم أي Skill متاح عندك يفيد المهمة** — للفرونت: skill التصميم (frontend-design) إجباري قبل أي شغل UI. للباك اند: أي skill يفيد في التوثيق أو السكافولدينج أو الاختبارات، استخدمه بدل ما تشتغل يدوي.
- **كل رسائل الأخطاء والواجهة بالعربي**، الكود والتعليقات التقنية بالإنجليزي (ده المعتاد ومفيش داعي نغيره).
- لو قابلت قرار معماري مش متغطي في الملفين ومحتاج اختيار — وقف واسأل بدل ما تفترض، خصوصًا لو القرار يمس الأمان أو الصلاحيات.

---

## الملفات المرفقة
- `backend.md` — كل تفاصيل الباك اند (.NET + SQL Server + Docker)
- `frontend.md` — كل تفاصيل الفرونت اند (React/Next.js)
