# Frontend — Gym Management SaaS

Stack: **React + Next.js (App Router)**

---

## 0. قواعد صارمة (Rules) — اقرأها قبل أي سطر كود

1. **قبل أي عمل تصميم/UI (صفحة، مكوّن، مودال، لاي أوت)، استخدم الـ 5 Skills دول بالترتيب ده بالظبط — مفيش أي إضافة أو استبدال:**
   1. **`frontend-design`** — الاتجاه العام للتصميم (Visual Direction): الألوان، الـ typography، الشخصية البصرية العامة للمنتج. يُقرأ الأول قبل أي قرار تصميمي.
   2. **`arrange`** — الـ layout والـ spacing والـ grouping والـ visual rhythm: إزاي العناصر بتترتب على الصفحة، المسافات بينها، التجميع المنطقي.
   3. **`shadcn/ui`** — الـ components والـ building blocks الفعلية (أزرار، مودالز، جداول، فورمات). استخدم مكونات الـ skill ده كأساس، ماتبنيش من الصفر لو موجود مكوّن جاهز يغطي الحاجة.
   4. **`ui-animation`** — الـ animations والـ transitions والـ springs والـ gestures والـ easing (Framer Motion). يُطبّق بعد ما شكل الصفحة الأساسي يستقر، مش قبل.
   5. **`Impeccable`** — المراجعة النهائية والـ polish: نقد وتدقيق وتحسين التفاصيل الدقيقة بعد ما الصفحة/المكوّن يخلص. يُستخدم كخطوة أخيرة على كل صفحة قبل ما تعتبرها "خلصت"، مش أول ما تبنيها.

   الترتيب مهم: `frontend-design → arrange → shadcn/ui → ui-animation → Impeccable`. اقرأ الـ skill المطلوب قبل ما تكتب أي JSX، مش بعد ما تخلص، وده ينطبق على كل صفحة جديدة من غير استثناء.
2. **الهدف: UI/UX احترافي وطبيعي، Design System متسق عبر كل الصفحات** — مش كل صفحة بشخصية بصرية مختلفة. لو فيه Skill تاني (غير الخمسة دول) بيساعد في حاجة تقنية بحتة (زي توثيق Next.js) استخدمه، لكن للتصميم بالذات الخمسة دول بس، من غير أي إضافة.
3. **Feature-based structure إجباري** — منطق كل فيتشر (API hooks, components, types) مع بعضه في فولدر واحد، مش متوزع حسب النوع (كل الـ components في مكان وكل الـ hooks في مكان).
4. **التوكنات (خصوصًا توكن impersonation) تتخزن في httpOnly cookie فقط.** ممنوع localStorage أو sessionStorage للتوكنات نهائيًا.
5. **الحماية على مستوى الـ Middleware (`middleware.ts`) قبل الـ render، مش بعده.** إخفاء زرار في الـ UI مش حماية كافية — أي صفحة تحت `(owner)` أو `(supervisor)` تتأكد من الـ role جوا التوكن قبل ما تفتح.
6. **الفورمات (react-hook-form + zod)** — الـ schema بتاع الـ validation في الفرونت لازم يطابق نفس قواعد الـ Validator بتاع الباك اند بالظبط (نفس رسائل الأخطاء بالعربي)، عشان الخطأ يظهر فورًا قبل حتى ما يتبعت request.
7. **Error Boundary عام** على مستوى الـ app — أي crash في أي component يترصد ويعرض صفحة "حصل خطأ غير متوقع" بدل شاشة بيضا.
8. **Toast/Notification موحّد** لكل أخطاء الـ API — نفس الشكل البصري في كل الأب، مش كل صفحة بتبني رسالتها لوحدها.
9. **GET requests بس تتعمل لها retry تلقائي** عند فشل الشبكة (1-2 محاولة). POST/PUT/DELETE ممنوع تتكرر تلقائيًا أبدًا.
10. **اللغة الأساسية عربي RTL** — كل الصفحات والفورمات والرسائل بالعربي، اتجاه الصفحة `dir="rtl"`.
11. **`API-CONTRACT.md` هو المصدر الوحيد لشكل أي request/response** — اقرأه قبل ما تكتب أي API hook، ماتفترضش شكل أي endpoint من اسمه أو تخمّنه. لو فيه تعارض بين إحساسك الشخصي وبين اللي مكتوب في `API-CONTRACT.md`، الملف هو اللي يكسب دايمًا — هو منسوخن (versioned) ومتزامن مع الباك اند الفعلي، مش وصف تقريبي.

---

## 0.5 Design System — مرجع إجباري لكل صفحة

قبل أي صفحة جديدة، تأكد إن العناصر دي متسقة مع أي صفحة اتعملت قبلها في نفس المشروع — الهدف إن المنتج يبان كأنه اتصمم مرة واحدة، مش مجموعة صفحات منفصلة:
- **Tokens ثابتة**: الألوان، الـ spacing scale، الـ border-radius، الـ shadows — تتحدد مرة واحدة (عن طريق `frontend-design`) وتتخزن كـ CSS variables/Tailwind config، وأي صفحة جديدة تستخدمها بدل ما تخترع قيم جديدة.
- **مكونات موحّدة**: زرار واحد، مودال واحد، جدول واحد (من `shadcn/ui`) بيتكرر استخدامهم في كل مكان — مش نسخة مختلفة لكل صفحة.
- **حالات موحّدة لكل مكوّن تفاعلي**: loading، empty، error، success — بنفس الأسلوب البصري عبر كل الصفحات (جدول المنشآت وجدول اللوجات مثلًا يوريان نفس شكل "مفيش بيانات" بالظبط).
- **RTL أولًا لا استثناء**: أي مكوّن من `shadcn/ui` أو أي animation من `ui-animation` لازم يتأكد إنه شغال صح في اتجاه RTL (الآيقونات، اتجاه الحركة، محاذاة النصوص) قبل ما يتعتبر جاهز.
- **مراجعة `Impeccable` مش اختيارية** — أي صفحة أو مكوّن جديد يعدّي عليها الخطوة دي قبل ما يتضاف كـ "مكتمل"، حتى لو شكلها كويس ظاهريًا.

---

## 1. هيكل المشروع

```
frontend/
├── src/
│   ├── app/                              # Next.js App Router
│   │   ├── (auth)/
│   │   │   ├── login/                    # إيميل+باسورد ثم التحقق من TOTP (نفس الصفحة، خطوتين)
│   │   │   ├── change-password/          # إجبارية لو mustChangePassword=true
│   │   │   └── contract-signing/         # صفحة العقد الإجبارية + التوقيع
│   │   ├── (owner)/
│   │   │   ├── onboarding/
│   │   │   └── dashboard/
│   │   │       ├── page.tsx              # الكروت + الرسم البياني (زي الصورة المرجعية)
│   │   │       ├── branches/
│   │   │       ├── members/
│   │   │       ├── subscriptions/
│   │   │       ├── plans/
│   │   │       ├── payments/
│   │   │       ├── staff/
│   │   │       ├── reports/
│   │   │       ├── audit-log/
│   │   │       ├── notifications/
│   │   │       └── settings/
│   │   ├── (supervisor)/
│   │   │   ├── facilities/               # جدول كل المنشآت
│   │   │   ├── facilities/[id]/          # تفاصيل منشأة + لوج + عقد
│   │   │   ├── facilities/new/           # فورم إنشاء منشأة جديدة
│   │   │   └── support-tickets/
│   │   └── layout.tsx
│   │
│   ├── features/
│   │   ├── auth/                         # لوجين + TOTP + change-password hooks
│   │   ├── owner-dashboard/
│   │   ├── facilities/
│   │   ├── audit-log/
│   │   ├── supervisor/
│   │   │   └── role-select-modal/        # مودال اختيار الـ Role عند الدخول كسوبرفايزر
│   │   ├── contracts/
│   │   ├── subscriptions/
│   │   ├── branches/
│   │   ├── players/
│   │   └── support-tickets/
│   │       # كل فيتشر: api/ (react-query hooks) + components/ + types/ + utils/
│   │
│   ├── shared/
│   │   ├── components/ui/                # عناصر عامة (Button, Modal, Table, Toast)
│   │   ├── hooks/
│   │   ├── lib/
│   │   │   ├── api-client.ts             # axios/fetch instance واحد، يفهم شكل الخطأ الموحّد من الباك اند
│   │   │   └── auth.ts
│   │   └── types/
│   │       └── api-error.ts              # يطابق شكل الـ response بتاع الباك اند بالظبط
│   │
│   ├── middleware.ts                     # حماية الـ routes حسب role التوكن
│   └── config/
│
├── public/
├── Dockerfile
└── next.config.js
```

---

## 2. صفحات مطلوبة بالتفصيل

### 2.0 تسجيل الدخول + التحقق من TOTP (`(auth)/login`)
- **خطوة 1**: إيميل + باسورد → `POST /api/auth/login/supervisor` أو `/login/owner` حسب نوع الحساب.
- **خطوة 2 (TOTP)**: لو الـ response رجّع `requiresTotpSetup: true`، اعرض الـ QR code (`totpSetupQrUri`) عشان يتمسح بـ Google Authenticator، وخانة إدخال الكود. لو `requiresTotpVerification: true` بس، اعرض خانة الكود مباشرة من غير QR.
  - ⚠️ **الفرونت يبعت `temporaryToken` اللي رجع من خطوة 1 زي ما هو، بدون أي تعديل، لـ `POST /api/auth/verify-totp` مع الكود.** الفرونت **ماعندوش وصول لأي إيميل أو سكرت خام** في الخطوة دي — كل حاجة متحكم فيها من السيرفر عن طريق `temporaryToken` نفسه.
- **بعد النجاح**: لو `mustChangePassword: true` في الـ response، حوّل المستخدم فورًا لصفحة `change-password` (قسم 2.0.1) قبل أي حاجة تانية — مايشوفش الداشبورد خالص لحد ما يغيّر الباسورد.

### 2.0.1 تغيير كلمة السر الإجباري (`(auth)/change-password`)
- تظهر تلقائيًا (redirect إجباري، مش اختيار) لو التوكن فيه `mustChangePassword: true`.
- فورم: الباسورد الحالي + الباسورد الجديد + تأكيد الباسورد الجديد.
- بعد النجاح، التوكن الجديد اللي بيرجع من `POST /api/auth/change-password` يستبدل القديم فورًا (القديم بقى ملغي من السيرفر تلقائيًا).
- الميدل وير (`middleware.ts`) يمنع الوصول لأي صفحة غير الصفحة دي طول ما `mustChangePassword: true` قائم في التوكن — نفس القاعدة المطبقة في الباك اند، مكررة هنا كطبقة دفاع إضافية على مستوى الفرونت (defense-in-depth، مش بديل عن فحص الباك اند).

### 2.1 مودال اختيار الدور (Supervisor impersonation)
- اختيار Role (Owner/BranchManager/Coach/Reception) + الفرع (لو أكتر من فرع)
- **رسالة تحذير ثابتة** واضحة: "إنت داخل على بيانات المنشأة الحقيقية، أي تعديل هيتسجل باسمك"
- **لا يوجد أي إشعار يوصل للمنشأة** — التحذير ده للسوبرفايزر نفسه بس
- شريط ثابت أعلى الشاشة طول ما السيشن شغالة: "أنت داخل كـ Supervisor — [اسم المنشأة]" + زرار خروج

### 2.2 داشبورد الـ Owner (الصفحة الرئيسية)
كروت علوية: **إجمالي المصروفات، إجمالي الإيرادات، صافي الربح (محسوب تلقائي، لون يتغير حسب موجب/سالب)، إجمالي المشتركين، إجمالي الفروع.**
(تنبيه: كارت "اشتراكات تقترب من الانتهاء" اتشال من هنا بقرار — لو احتجته يرجع، يبقى في صفحة الاشتراكات بدل الداشبورد الرئيسي)

باقي الصفحة: رسم بياني (مصروفات vs إيرادات)، أداء الفروع، آخر الاشتراكات، الإشعارات الحديثة.

### 2.3 صفحة العقد الإلكتروني (`contract-signing`)
- عرض نص العقد الحالي (النسخة الفعّالة، مش نسخة قديمة)
- checkbox إجباري + زرار "أوافق وأقر" (غير مفعّل لحد ما الـ checkbox يتحدد)
- خانة إدخال الاسم الكامل → معاينة فورية بخط توقيع (font يد) قبل التأكيد
- منع كامل من مغادرة الصفحة أو فتح أي رابط تاني قبل التوقيع

### 2.4 جدول المنشآت (Supervisor)
أعمدة: اسم المنشأة + المالك، حالة الاشتراك (نشط/موقوف/تجريبي/مبيع)، تاريخ الاشتراك والانتهاء، زرار تفعيل/إيقاف.
فلترة: بالتاريخ، بالمستخدم، بنوع الحركة (للوجات الداخلية).

### 2.5 صفحة تفاصيل منشأة (Supervisor)
تبويبات: بيانات عامة، الـ Audit Log الخاص بيها، حالة الاشتراك/القفل، العقد الموقّع (+ زرار تصدير PDF)، سجل المدفوعات المستلمة منها.

---

## 3. Error Handling (Frontend)

### 3.1 `api-client.ts` — نقطة مركزية واحدة
كل response من الباك اند بشكل:
```json
{ "success": false, "statusCode": 400, "message": "...", "errors": {...}, "correlationId": "..." }
```
يُترجم فورًا لـ:
- `errors` (لو موجودة) → تُوزَّع على حقول الفورم مباشرة (react-hook-form `setError`)
- مفيش `errors` بس فيه `message` → Toast موحّد
- `401` → تسجيل خروج فوري وتحويل للوجين (إلا لو `error_code: IMPERSONATION_EXPIRED` → مودال تجديد سيشن بدل الطرد)
- `403` → Toast "لا تملك صلاحية" بدون logout
- `423` → صفحة مخصصة "الحساب مجمّد، تواصل مع الإدارة" (مش توست عابر)
- `409` → Toast "البيانات اتغيرت، جاري التحديث" + إعادة تحميل البيانات تلقائيًا
- `500/503` → Toast عام + عرض `correlationId` بشكل صغير تحت الرسالة (يفيد وقت فتح تيكت دعم)

### 3.2 Error Boundary
مكوّن واحد على مستوى `app/layout.tsx` يغلّف كل الصفحات، يعرض واجهة "حصل خطأ، حاول تاني" مع زرار Reload، بدون كشف أي تفاصيل تقنية للمستخدم.

---

## 4. أوامر البداية المتوقعة
```
npx create-next-app@latest frontend --typescript --tailwind --app --src-dir
cd frontend
npm install @tanstack/react-query axios react-hook-form zod @hookform/resolvers
```
